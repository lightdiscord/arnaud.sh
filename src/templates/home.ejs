<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>arnaud.sh</title>
  </head>
  <body>
    <header class="py-20">
      <div class="container mx-auto p-4">
        <h1>
          Arnaud <span class="text-2xl text-gray-400">lightdiscord</span>
        </h1>

        <p>
          I'm a student learning programming and hacking. Some of my work is
          available on my <a href="https://github.com/LightDiscord">GitHub</a>.
          I'm also enjoying CTF challenges and doing some on 
          <a href="https://www.root-me.org/LightDiscord">root-me</a>.
        </p>
      </div>
    </header>

    <div class="container mx-auto p-4">
      <section>
        <h2>Some articles</h2>

        <div class="mt-4 flex flex-col gap-y-6">
          <% const { articles } = htmlWebpackPlugin.options %>
          <% articles.forEach((article) => { %>
          <div>
              <p class="font-bold"><%= article.title %></p>
              <p><%= article.description %></p>
              <a href="/articles/<%= article.slug %>/">
                See article
              </a>
          </div>
          <% }) %>
        </div>
      </section>
    </div>
  </body>
</html>
