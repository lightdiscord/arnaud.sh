export const storeRepositories = (state, repositories) => {
    state.repositories = repositories
}

export const storeOrganizations = (state, organizations) => {
    state.organizations = organizations
}
